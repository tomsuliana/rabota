<h1>Administration panel</h1>
<div class="mt-2 d-flex align-items-center">
    <a href="<%= new_question_path %>" class="ms-1 link text-primary">Create New Question</a>
</div>
<div class="mt-2 d-flex align-items-center">
  <a href="<%= users_path %>" class="ms-1 link text-primary"> Show users </a>
</div>
<%= form_with url: "/session/logout", method: :get do |form| %>
  <%= form.submit('Выход', id: 'exit-btn') %>
<% end %>
